# ===========================================
# Backend Server Environment — Example
# ===========================================
# Copy to backend/.env and fill in real values.
# NEVER commit backend/.env — it contains production secrets.

# ===========================================
# API KEYS
# ===========================================
GEMINI_API_KEY=your_gemini_api_key
PINATA_API_KEY=your_pinata_api_key
PINATA_SECRET_KEY=your_pinata_secret_key

# ===========================================
# BLOCKCHAIN (server-side RPC — may contain API key)
# ===========================================
BLOCKCHAIN_RPC_URL=https://polygon-mainnet.infura.io/v3/your_infura_key

# ===========================================
# DATABASE
# ===========================================
DATABASE_URL=postgresql://user:password@localhost:5432/morningstar_credentials
REDIS_URL=redis://localhost:6379
CACHE_TTL=3600

# ===========================================
# AUTH & SESSIONS
# ===========================================
JWT_SECRET=generate_a_secure_random_string
SESSION_SECRET=generate_another_secure_random_string
SESSION_TIMEOUT=3600000

# ===========================================
# MONITORING (server-side)
# ===========================================
SENTRY_DSN=
LOG_LEVEL=info
LOG_FORMAT=json
APM_ENABLED=false
TRACE_SAMPLE_RATE=0.1

# ===========================================
# SMS / TWILIO
# ===========================================
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

# ===========================================
# EMAIL / SMTP
# ===========================================
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=Morningstar Credentials <noreply@yourcompany.com>

# ===========================================
# RATE LIMITING
# ===========================================
RATE_LIMIT_WINDOW=60000
RATE_LIMIT_MAX_REQUESTS=100
RATE_LIMIT_MAX_CREDENTIAL_ISSUANCE=10

# ===========================================
# DATA RETENTION (days)
# ===========================================
CONSENT_RETENTION_DAYS=730
AUDIT_LOG_RETENTION_DAYS=2555
CREDENTIAL_CACHE_DAYS=90

# ===========================================
# SERVER
# ===========================================
PORT=3001
HOST=0.0.0.0
ALLOWED_ORIGIN=http://localhost:3000
SSL_CERT_PATH=
SSL_KEY_PATH=
NODE_ENV=development
