# Backend Environment Example
# Copy to backend/.env
# Never commit backend/.env with real secrets.

# -----------------------------------------------------------------------------
# External API credentials (only needed if those endpoints are used)
# -----------------------------------------------------------------------------
GEMINI_API_KEY=
PINATA_API_KEY=
PINATA_SECRET_KEY=

# -----------------------------------------------------------------------------
# Auth/session
# -----------------------------------------------------------------------------
AUTH_TOKEN_SECRET=replace-with-strong-random-secret
# Optional legacy alias (AUTH_TOKEN_SECRET preferred)
JWT_SECRET=

ACCESS_TOKEN_TTL_SECONDS=900
REFRESH_TOKEN_TTL_SECONDS=604800
AUTH_SESSION_TTL_SECONDS=86400
ALLOW_MOCK_WALLET_BINDING=true

# Governance bootstrap wallet allowlist (comma-separated lowercase 0x...)
GOVERNANCE_BOOTSTRAP_WALLETS=

# -----------------------------------------------------------------------------
# Student verification and trust controls
# -----------------------------------------------------------------------------
STUDENT_OTP_TTL_SECONDS=300
STUDENT_OTP_MAX_ATTEMPTS=5
RISK_SCORE_STEPUP_THRESHOLD=70

UNIVERSITY_DOMAIN_DATASET_URL=https://raw.githubusercontent.com/Hipo/university-domains-list/master/world_universities_and_domains.json
UNIVERSITY_ALLOWED_DOMAINS=
# Optional alias for compatibility
UNIVERSITY_DOMAINS=

DISPOSABLE_EMAIL_DOMAINS=
DISPOSABLE_DOMAIN_BLOCKLIST_URL=

TURNSTILE_SECRET_KEY=
TURNSTILE_REQUIRED=false

# -----------------------------------------------------------------------------
# Email / SMTP
# -----------------------------------------------------------------------------
# auto: use SMTP when fully configured, else mock
# smtp: require SMTP config
# mock: always mock
EMAIL_TRANSPORT_MODE=mock

SMTP_PROVIDER=sendgrid
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=apikey
SMTP_PASSWORD=
SMTP_FROM=Morningstar Credentials <noreply@example.com>

EXPOSE_EMAIL_HEALTH_DETAILS=false

# Optional SMS provider for MFA endpoints
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

# -----------------------------------------------------------------------------
# Server networking
# -----------------------------------------------------------------------------
NODE_ENV=development
PORT=3001
HOST=0.0.0.0

ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:5173,http://127.0.0.1:5173,http://localhost:8080
# Optional alias for compatibility
ALLOWED_ORIGIN=

TRUST_PROXY=
REQUEST_LOGS=false

# -----------------------------------------------------------------------------
# Request and response tuning
# -----------------------------------------------------------------------------
JSON_BODY_LIMIT=512kb
EXTERNAL_API_TIMEOUT_MS=20000
REQUEST_TIMEOUT_MS=30000
KEEP_ALIVE_TIMEOUT_MS=65000
HEADERS_TIMEOUT_MS=66000
MAX_REQUESTS_PER_SOCKET=1000
SLOW_REQUEST_THRESHOLD_MS=800

ENABLE_RESPONSE_COMPRESSION=true
RESPONSE_COMPRESSION_MIN_BYTES=1024
HEALTH_CACHE_MAX_AGE_SECONDS=5

# -----------------------------------------------------------------------------
# Rate limiting
# -----------------------------------------------------------------------------
RATE_LIMIT_WINDOW=60000
RATE_LIMIT_MAX_REQUESTS=120

RATE_LIMIT_WINDOW_AUTH_SESSIONS=900000
RATE_LIMIT_MAX_AUTH_SESSIONS=20
RATE_LIMIT_WINDOW_AUTH_OTP=900000
RATE_LIMIT_MAX_AUTH_OTP=10
RATE_LIMIT_WINDOW_AUTH_ROLE=900000
RATE_LIMIT_MAX_AUTH_ROLE=20

RATE_LIMIT_WINDOW_EXTERNAL=60000
RATE_LIMIT_MAX_EXTERNAL_REQUESTS=30

RATE_LIMIT_WINDOW_DID_WRITES=60000
RATE_LIMIT_MAX_DID_WRITES=25
RATE_LIMIT_WINDOW_GOVERNANCE_WRITES=60000
RATE_LIMIT_MAX_GOVERNANCE_WRITES=25
RATE_LIMIT_WINDOW_BLOCKCHAIN_WRITES=60000
RATE_LIMIT_MAX_BLOCKCHAIN_WRITES=40

RATE_LIMIT_WINDOW_MFA=900000
RATE_LIMIT_MAX_MFA_ATTEMPTS=5
